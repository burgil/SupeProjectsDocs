{
  "scripts": {
    "start": "vitepress dev docs",
    "publish": "npm run build && npx wrangler pages deploy ./docs/.vitepress/dist --project-name=supeprojects",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.4.1"
  }
}
